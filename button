package com.thehowtotutorial.splashscreen;import java.awt.EventQueue;import java.awt.Image;import java.awt.event.ComponentAdapter;import java.awt.event.ComponentEvent;import java.awt.event.ComponentListener;import java.awt.event.ActionEvent;import java.awt.event.ActionListener;import java.awt.event.*;import javax.swing.*;import javax.swing.ImageIcon;import javax.swing.JFrame;import javax.swing.JLabel;import javax.swing.JButton;import javax.swing.JTextArea;import java.awt.Color;import java.awt.BorderLayout;import java.awt.event.MouseAdapter;import java.awt.event.MouseEvent;import java.awt.CardLayout;import java.awt.GridLayout;public class SelectStation extends JFrame {    private JButton myLabel;        //이벤트 발생시    /*public void actionPerformed(ActionEvent e) {        SelectStation SelectStation = new SelectStation();       SelectStation.Select_Station();             /*지하철 노선도 보여주고 선택하도록 하는 것*/      //}    public static void main(String[] args){     new ImageResize();    }    public SelectStation(){        setTitle(" How can I find seat fast ");        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);        setSize(837, 517);        setLocationRelativeTo(this);        getContentPane().setLayout(null);        myLabel = new JButton(new ImageIcon("C:\\Users\\user1\\Desktop\\SelectStation.PNG"));        myLabel.addActionListener(new ActionListener() {         public void actionPerformed(ActionEvent e) {          //         }        });        myLabel.setForeground(Color.WHITE);        myLabel.setBackground(Color.WHITE);        myLabel.setHorizontalAlignment(JLabel.CENTER);               // myLabel.addActionListener(this);        myLabel.addComponentListener(new ComponentAdapter() {            @Override            public void componentResized(ComponentEvent e) {                int width = myLabel.getWidth();                int height = myLabel.getHeight();                ImageIcon icon = new ImageIcon(getClass().getResource("/image/example.jpg"));                Image img = icon.getImage();//이미지 보여주는 것                           GraphicThread gt=new GraphicThread();                gt.setSize(300,200);                gt.setVisible(true);                Thread t=new Thread(gt);  //Thread 클래스를 생성한다.                t.start();   //run메서드로 호출한다.                                myLabel.setIcon(new ImageIcon(img.getScaledInstance(width, height, Image.SCALE_SMOOTH)));            }        });                JButton btnNewButton = new JButton("New button");        btnNewButton.addActionListener(new ActionListener() {         public void actionPerformed(ActionEvent e) {         }        });        getContentPane().setLayout(new GridLayout(0, 1, 0, 0));        getContentPane().add(btnNewButton);                JLabel label = new JLabel("");        getContentPane().add(label);                JLabel label_1 = new JLabel("");        getContentPane().add(label_1);                JLabel label_2 = new JLabel("");        getContentPane().add(label_2);                JButton btnNewButton_1 = new JButton("New button");        getContentPane().add(btnNewButton_1);                JButton btnNewButton_2 = new JButton("New button");        getContentPane().add(btnNewButton_2);        getContentPane().add(myLabel);                getContentPane().setLayout(null);         }  }
