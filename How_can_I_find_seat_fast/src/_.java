import java.awt.Color;
import java.awt.Image;

import javax.swing.ImageIcon;

public class _ {
	while(GetCurrentTimeExam()<목적지도착시간){
		if(stop == true)//멈췄다면
		{
			//해당하는 역의 이미지 보여줘 
			if(i==0)현재시간일때 나는? 
			else
					나+1의 이미지를 보여줘 
			
		}
		else//가는 중이라면
		{
			for(int j = 0 ; j< 10??;j++){   
			ImageIcon doorIcon = new ImageIcon("img/subway_door"+j+".png");//이름 이렇게 바꿔서 저장할 것 
			      Image doorImg = doorIcon.getImage();
			      g.drawImage(doorImg, boundedRect.x+45, boundedRect.y, boundedRect.width-120,boundedRect.height, null);//이부분 +40
			      g.setColor(Color.black); 
			}
		}
	}
}
